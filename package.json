{
  "name": "green-screen",
  "private": true,
  "workspaces": [
    "packages/types",
    "packages/react",
    "packages/proxy",
    "apps/demo",
    "apps/worker"
  ],
  "scripts": {
    "dev": "concurrently --kill-others -n proxy,demo -c blue,green \"node --watch --import tsx/esm packages/proxy/src/server.ts\" \"cd apps/demo && npx vite\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
